ALTER TABLE MEMBER_TB ADD IS_ADMIN NUMBER DEFAULT 0 NOT NULL;

- 백엔드
npm install nodemailer

- 프론트엔드
npm install react-icons

CREATE TABLE SITE_THEME_TB (
    THEME_IDX NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    PRIMARY_TONE VARCHAR2(50) DEFAULT 'BLUE' NOT NULL,
    IS_DARK_MODE CHAR(1) DEFAULT 'N' NOT NULL,
    UPDATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
);
INSERT INTO SITE_THEME_TB (primary_tone, is_dark_mode) VALUES ('blue', 'n');
commit;